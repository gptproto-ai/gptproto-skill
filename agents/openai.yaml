interface:
  display_name: "GPTProto CLI"
  short_description: "Discover GPTProto interfaces and execute their native request bodies through the CLI"
  default_prompt: "Use $gptproto-skill to discover the right GPTProto interface and execute my request through GPTProto CLI."
